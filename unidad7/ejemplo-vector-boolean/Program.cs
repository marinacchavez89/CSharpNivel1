using System;

namespace ejemplo_vector_boolean
{
    class Program
    {
        static void Main(string[] args)
        {
            // Por ejemplo hacre programa para contabilizar asistencias de 100 aalumnos a las 16 clases.

            int[] legajos = new int[100];
            bool[] asistencia = new bool[100];

            for (int x = 0; x < 16; x++)
            {
                for (int y = 0; y < 100; y++)
                {
                    // aca se hacen los procesos
                }

                // se pueden leer los vectores y mostrar resultados de ser necesario.
                // Procesar tambien la info.
            }
        }
    }
}
